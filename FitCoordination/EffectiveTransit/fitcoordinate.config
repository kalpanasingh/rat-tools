# -*- python-*-

description = "Coordinate the EffectiveTransit, transit time calculator. Runs one RAT script, fairly quick."
production_script = "ProduceFiles.py"
analysis_script = "AnalyseFiles.py"
