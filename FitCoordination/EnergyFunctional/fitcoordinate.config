# -*- python-*-

description = "Coordinate the Functional Energy Fitter - calculate the values to be placed into the fitter's RATDB file."
production_script = "ProduceData.py"
analysis_script = "AnalyseData.py"
