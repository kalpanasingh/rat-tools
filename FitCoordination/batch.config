submit = "qsub" # Basic submission command (may require e.g. "-q queue-name" option
preamble = ["#!/bin/bash"] # Preamble required/desired at top of batch script
ratenv = "/home/env_rat-dev.sh" # Location of rat environment to source
