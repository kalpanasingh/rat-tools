# -*- python-*-

description = "Coordinate the EmissionTimePDF (ET1D) - runs one RAT script, and uses the effective transit time approximation."
production_script = "ProduceData.py"
analysis_script = "AnalyseData.py"
