# -*- python-*-

description = "Coordinate the EmissionTimePDF method. Runs one RAT script, fairly quick. This uses the effective transit time approximation."
production_script = "ProduceFiles.py"
analysis_script = "AnalyseFiles.py"
